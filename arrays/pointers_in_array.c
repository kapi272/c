// using the pointers in array to acces the induvale values of the array

#include<stdio.h>

int main(){

    int arr[] = {10, 20, 30, 40, 50};
    
    int *ptr = arr;
    printf("%d\n", *ptr);
    
    
}