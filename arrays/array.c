//==================================================================================================================//
//               Collection of similar data types stored at a "contiguous memory" location                          //
//==================================================================================================================//
/*
    -> Should always use the elements within the INDEX VALUE other wise will give " run time error "

    -> c is zero based indexing language

    -> if array length is ' n ' then valid index is ' 0 ' to ' n-1 '

    -> DataType array[Size of array];
       ex : int mark[10];

    -> input    scanf("%DataType", &array[index]);

    -> output   printf("%DataType", array[index]);

*/